<configuration>

    <!-- appender configuration -->
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <withJansi>true</withJansi>
        <encoder>
            <pattern>%date %highlight(%5.5level) --- [%thread] %green(%logger) --- [%file:%line] %msg%n</pattern>
            <charset>utf8</charset>
        </encoder>
    </appender>

    <logger name="com.fuchun.lianjiacrawler" level="DEBUG">
        <appender-ref ref="console"/>
    </logger>

    <!--<root level="DEBUG">-->
        <!--<appender-ref ref="console"/>-->
    <!--</root>-->

</configuration>